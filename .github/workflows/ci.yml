name: CI

on:
  push:
    branches: [ main ]
  pull_request:
    branches: [ main ]

jobs:
  test:
    runs-on: ubuntu-latest

    steps:
      - name: Checkout repository
        uses: actions/checkout@v4
        with:
          fetch-depth: 0

      - name: Setup Node.js
        uses: actions/setup-node@v4
        with:
          node-version: '24'
          cache: 'npm'

      - name: Install dependencies
        run: npm ci

      - name: Run Backend Tests (Changes Only)
        run: npm run test:backend -- --changedSince=origin/main

      - name: Run Frontend Tests (Changes Only)
        run: npm run test:frontend -- --changedSince=origin/main